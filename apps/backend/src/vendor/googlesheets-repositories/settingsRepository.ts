// AUTO-GENERATED by `npm run sync:backend-vendor` from packages/googlesheets-repositories/src - do not edit here.
// Edit the source in packages/googlesheets-repositories instead, then re-run that script to update this copy.
import type { sheets_v4 } from 'googleapis';
import type { SettingsRepository } from '../core/index.js';
import { SheetTable, type FieldSpec } from './sheetTable.js';

interface SettingsRow extends Record<string, unknown> {
  id: string; // reuse SheetTable's id-keyed logic: id === key
  value: unknown;
}

const FIELDS: FieldSpec[] = [
  { key: 'id', type: 'string' },
  { key: 'value', type: 'json', optional: true },
];

export class GoogleSheetsSettingsRepository implements SettingsRepository {
  private table: SheetTable<SettingsRow>;

  constructor(sheets: sheets_v4.Sheets, spreadsheetId: string, tabName = 'Settings') {
    this.table = new SheetTable(sheets, spreadsheetId, tabName, FIELDS);
  }

  async get<T = unknown>(key: string): Promise<T | undefined> {
    const row = await this.table.getById(key);
    return row?.value as T | undefined;
  }

  async set<T = unknown>(key: string, value: T): Promise<void> {
    const existing = await this.table.getById(key);
    if (existing) {
      await this.table.update(key, { value });
    } else {
      await this.table.create({ id: key, value });
    }
  }

  async remove(key: string): Promise<void> {
    await this.table.remove(key);
  }
}
